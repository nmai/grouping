'use strict'

import * as should from 'should'
import { PeopleFilter } from '../src/index'

describe('index.js', () => {
  describe('normalizeRatios', () => {
    it('3 ratios with 0 remainder', () => {
      let filter = new PeopleFilter()
    })
  })
})
